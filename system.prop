# ADB
ro.adb.secure=0
ro.boot.dynamic_partitions=true

# Gatekeeper (Disable SPU, fallback to TEE for TWRP Decryption)
vendor.gatekeeper.disable_spu=true
vendor.gatekeeper.is_security_level_spu=0

# Battery
ro.charger.enable_suspend=1
